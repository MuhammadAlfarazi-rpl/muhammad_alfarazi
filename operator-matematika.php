<?php

    $x = 100;
    $y = 10;

    $penjumlahan = $x + $y;
    $pengurangan = $x - $y;
    $perkalian = $x * $y;
    $pembagian = $x / $y;
    $sisa_bagi = $x % $y;
    $pemangkatan = $x ** $y;

    $lebih_besar = $x > $y;
    $tidak_sama = $x != $y;
    $identik = $x === $y;

    $and = ($x > 99) && ($y < 10);
    $or = ($x > 99) || ($y < 10);
    $not = !($x == $y);

    ?>